{
  "name": "visual-totp-simulator",
  "version": "1.0.0",
  "description": "An interactive visual tool that explains how TOTP works internally",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
